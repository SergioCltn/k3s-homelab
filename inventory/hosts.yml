all:
  children:
    k3s:
      children:
        k3s_servers:
        k3s_agents:
    k3s_servers:
      hosts:
        aetherion:
          ansible_host: 192.168.1.134
          ansible_user: aetherion
    k3s_agents:
      hosts:
        # Disabled: host is currently unreachable on the LAN.
        # aetherion-worker-1:
        #   ansible_host: 192.168.1.35
        #   ansible_user: aetherion
