apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spend-app-postgres-backups
  namespace: spend-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: spend-app-postgres-backup
  namespace: spend-app
spec:
  schedule: "0 3 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: pg-dump
              image: public.ecr.aws/docker/library/postgres:16
              envFrom:
                - secretRef:
                    name: spend-app-postgres
              command:
                - sh
                - -ceu
                - |
                  export PGPASSWORD="$POSTGRES_PASSWORD"
                  mkdir -p /backups
                  timestamp=$(date +%Y%m%d-%H%M%S)
                  outfile="/backups/spend-app-${timestamp}.sql.gz"
                  pg_dump -h spend-app-postgres -U "$POSTGRES_USER" -d "$POSTGRES_DB" | gzip -9 > "$outfile"
                  find /backups -type f -name 'spend-app-*.sql.gz' -mtime +7 -delete
              volumeMounts:
                - name: backups
                  mountPath: /backups
          volumes:
            - name: backups
              persistentVolumeClaim:
                claimName: spend-app-postgres-backups
