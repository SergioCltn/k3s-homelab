apiVersion: v1
kind: Service
metadata:
  name: trading-bot-dashboard
  namespace: trading-bot-dashboard
spec:
  selector:
    app: trading-bot-dashboard
  ports:
    - name: http
      port: 8080
      targetPort: http
